machine:
  node:
    version: 6.1.0
dependencies:
  pre:
    - echo "//registry.npmjs.org/:_authToken=$NPM_TOKEN" >> ~/.npmrc
test:
  pre: 
    - npm run lint
  post:
    - npm run cover
deployment:
  npm:
    tag: /[0-9]+(\.[0-9]+)*/
    owner: Janus-vistaprint
    commands:
      - npm publish